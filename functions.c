#include "functions.h"
#include <stdio.h>
#include <stddef.h>

/* Declarations of solution implementations */
unsigned long long mystrlen(const char* str){

    return 0;
}


int mystrcmp(const char *lhs, const char *rhs) { return 0; }
char *mystrstr(const char* str, const char* substr) { return NULL; }
unsigned int count_ones(int num) { return 0; }
void int_to_arr(unsigned int num, int arr[], unsigned int sz) { return; }
